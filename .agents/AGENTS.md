# Project Rules & Guidelines

- **Git Branch Scoping**: Do NOT automatically push or merge changes into the `main` branch. Always perform work, commits, and pushes on the active working version branch (e.g., `1.0.3`). Only merge or push to `main` when explicitly commanded by the user.
