# 『アークナイツ：エンドフィールド』武器ターゲットサーチ＆フィルターシステム 機能仕様書

本仕様書は、HTML5/CSS3/JavaScriptおよびAndroid Kotlinネイティブコンテナにより構築された「武器ターゲットサーチ＆フィルターシステム」の機能・デザイン・データ構造・ビルドパイプラインにおける最新仕様（v1.0.6）をまとめたものです。

---

## 1. アプリケーション概要
本アプリは、3Dリアルタイム戦略RPG『アークナイツ：エンドフィールド（タロII）』のゲームシステムを想定した非公式ファンツールです。
各武器が持つバフ効果（基礎効果・付加効果・スキル効果）のドロップエリア内での重複（シナジー）を解析し、最適なフィルター設定を提案する「**ターゲットサーチ**」と、全武器データを一元閲覧・検索できる「**武器データベース**」の2つの主要機能を提供します。
UI/UXデザインは、ゲーム公式のアンバーイエロー（#f2a900）とネオンシアン（#00d2ff）を基調としたSF近未来HUDスタイルを再現しています。

---

## 2. 技術スタック
*   **Androidネイティブアウター**:
    *   **言語/フレームワーク**: Kotlin / Jetpack Compose
    *   **ビューアー**: `AndroidView` による WebView レンダリング
    *   **アセット読み込み**: `WebViewAssetLoader` を使用し、ローカルの `assets` 内リソースをHTTPSコンテキスト（`https://appassets.androidplatform.net/assets/index.html`）で安全に読み込み
    *   **署名管理**: 固定デバッグキー (`app/debug.keystore`) をビルド設定に組み込み、更新時のパッケージ・署名競合 (`INSTALL_FAILED_UPDATE_INCOMPATIBLE`) を防止
    *   **画面設定**: 縦画面固定（`screenOrientation="portrait"`）
*   **Webアプリケーション（コア）**:
    *   **マークアップ/スタイル**: HTML5 / Vanilla CSS3（Google Fonts: Outfit, Noto Sans JP）
    *   **ロジック**: Vanilla JavaScript (ES6)
    *   **永続化**: `localStorage` を使用したユーザーカスタム・同期済武器データのローカル保存

---

## 3. 画面構成と機能仕様

### 3.1 共通ヘッダー（HUDデザイン）
*   **デザイン**: 不透明なマットブラック背景（`#0b0c0e`）、1pxの境界線。スクロール時も画面上部に固定（`position: sticky`）され、背景コンテンツが透けないように遮断（`z-index: 1000`）。
*   **表示要素**: 
    *   「Endfield」のアンバーゴールド発光バッジ
    *   アプリタイトル（「武器ターゲットサーチ」）
    *   ナビゲーションタブ（「📡 ターゲットサーチ」「📂 武器データベース」）
    *   **✨ 更新確認ボタン**: タブ切替の隣に常時表示。タップすると0秒で即座にアプリ更新ダイアログを起動。

---

### 3.2 「ターゲットサーチ」タブ
選択した「基準武器」とドロップエリアが重なり、かつ各種効果が一致する他の武器を効率的に同時収集するための画面です。

*   **基準武器の選択**:
    *   **ドロップダウンセレクト**: 武器種ごとにグループ分け（`optgroup`）されたリストから基準武器を選択可能。
    *   **クイック選択リスト（左カラム）**: 登録されている全武器（64種）を一覧で縦スクロール表示。タップすることで瞬時に基準武器を切り替え可能。ヘッダー部に「クイック選択 (全64種)」と総数を自動カウント表示。
*   **基準武器カード表示**:
    *   選択された武器の名前、レアリティ（★5/★6）、モチーフキャラ、武器種、および効果テキスト（基礎、付加、スキル）をカード形式で表示。
*   **ドロップフィルター推奨設定（オプティマイザー）**:
    *   ゲーム内のドロップ仕様（基礎効果から3種、付加/スキルから1種をフィルター選択可能）に基づき、同じエリアでドロップする他武器（シナジー武器）を最も多く同時収集できる設定を自動算出・提案します。
    
    <div align="center">
      <img src="screenshots/target_search_optimizer.png" width="320" alt="ターゲットサーチ画面">
    </div>

*   **この組み合わせで一緒に狙える武器（右カラム）**:
    *   基準武器の入手可能エリアごとにコンテナ（📍マーク）を生成。推奨ドロップフィルター設定を満たし、そのエリアで実際に出現する適合武器のみを抽出してリスト表示。
    
    <div align="center">
      <img src="screenshots/target_search_list.png" width="320" alt="一緒に狙える武器リスト">
    </div>

---

### 3.3 「武器データベース」タブ
登録されている全武器データを表形式で閲覧・検索できます。

*   **データベース絞り込みフィルター（アコーディオン式）**:
    *   **初期状態**: 画面占有を避けるため、初期ロード時は折りたたまれた状態で表示。「データベース絞り込みフィルター 🔽」ヘッダー部をタップすることでスライドアニメーション開閉。
    
    <div align="center">
      <img src="screenshots/filter_collapsed.png" width="260" alt="閉じた状態">
      &nbsp;&nbsp;
      <img src="screenshots/filter_expanded.png" width="260" alt="展開された状態">
    </div>

*   **データベースコントロール（上部ボタン群）**:
    *   **🟩 スプレッドシート連携**: 固定Googleスプレッドシート（Tab 1 & Tab 2）から最新データを手動即時同期。
    *   **✨ 更新確認**: アプリの最新バージョンチェックおよび更新ダウンロード用ダイアログを起動。
    *   ※不要な「新規武器追加」および「リセット」ボタンは削除され、シンプルな操作体系に整理。
*   **データベーステーブル**:
    *   **表示列**: 「武器名 / 入手エリア」「武器種」「モチーフキャラ」「操作（詳細）」の4列構成。武器名の下に入手エリアタグバッジを一覧表示。
    *   **★6/★5背景色分け**: レアリティ列を廃止し、行の背景色と左端ボーダー（★6: オレンジ、★5: ゴールド）で判別。
    
    <div align="center">
      <img src="screenshots/table_rarity_rows.png" width="320" alt="データベーステーブル">
    </div>

---

### 3.4 モーダル（ダイアログ）仕様

*   **Googleスプレッドシート連携＆起動時自動同期**:
    *   **固定スプレッドシートURL**: `https://docs.google.com/spreadsheets/d/1dZGyJHG_oK9u5ocKmpHjThh76qvFk0knys9LYsR5Koo/edit`
    *   **2タブ自動クロスリファレンス**: 武器マスターシート（Tab 1 `gid=332980878`）とエリア別効果シート（Tab 2 `gid=142181474`）を自動取得し、エリアごとのバフ効果プール（基礎・付加・スキル）と照合して入手エリアを全自動アサイン。
    *   **ピリオド（`.`）トークン分割**: `電磁ダメージUP.寒冷ダメージUP` 等の複合効果テキストも正確に分解してマッチング。
    *   **全自動起動時差分更新**: アプリ起動時（`DOMContentLoaded`）、バックグラウンドで自動的にスプレッドシートの最新データを読み込み、差分があればローカルDBおよび画面表示を即座に更新。

*   **武器詳細情報モーダル**:
    *   武器のすべての情報（効果詳細、全入手エリアバッジ等）をカードリスト形式で表示。
    
    <div align="center">
      <img src="screenshots/detail_modal.png" width="280" alt="武器詳細モーダル">
    </div>

*   **アプリのアップデートモーダル**:
    *   ボタンタップ時、即座（0秒）にダイアログが開いて「⏳ 最新バージョンの情報を確認中...」と表示。
    *   GitHub RAW (`raw.githubusercontent.com/halver/endfield-weapon-filter/main/update.json`) および Gist から最新バージョン（v1.0.6等）を取得。
    *   新バージョンが利用可能な場合は「🎉 新しいバージョンが利用可能です！」と「アップデート実行」ボタンを表示し、タップでダウンロード＆直接インストールを実行。

---

## 4. アセットおよびアプリアイコンの管理

### 4.1 アプリアイコン仕様
*   ダークメタリックのスクエアを背景に、ネオンに光るアンバーゴールドの山形（シェブロン）と幾何学的なHUD武器照準デザインを配したアイコン。

<div align="center">
  <img src="screenshots/app_icon_base.png" width="180" alt="アイコンベース">
  &nbsp;&nbsp;&nbsp;&nbsp;
  <img src="screenshots/home_launcher_icon.png" width="220" alt="ホーム画面アイコン適用">
</div>

---

## 5. 改訂履歴

### v1.0.6
*   **初期データへのリセットボタンおよび新規武器追加機能の削除**: UIおよびコードから不要な「リセット」「新規武器追加」を完全削除。
*   **スプレッドシート最新データ（全64種＋エリア全自動アサイン）の `data.js` 同梱**: オフライン初期データ `app/src/main/assets/data.js` をスプレッドシートの最新64武器データで更新。
*   **アプリ起動時の全自動スプレッドシート差分チェック＆サイレント更新**: アプリ起動時に自動で最新データをバックグラウンド検出し、変更があればダイナミック更新。
*   **「更新確認」ボタンの即時ダイアログ起動＆常時表示仕様の復元**: タップした瞬間に0秒でダイアログが開く構造に戻し、通信状態や結果をリアルタイム表示。
*   **バージョン作成スクリプト (`create_version_branch.ps1`) の全自動化**: 新バージョン作成時にスプレッドシートから最新データを取得し `data.js` も自動更新する仕組みに拡張。

### v1.0.5
*   **ヘッダーナビゲーションへの「更新確認」ボタン追加**: どの画面からでもワンタップでアプリアップデートを確認可能。
*   **2タブ連携（Tab 2 / GID 142181474）のCORS安全化＆ピリオド区切り対応**: CORS制限のないCSVエクスポートURLを優先取得し、ピリオド（`.`）連結の付加効果トークン分割に対応。

### v1.0.4
*   **固定署名キー (`app/debug.keystore`) の適用**: ビルド間のパッケージ・署名競合 (`INSTALL_FAILED_UPDATE_INCOMPATIBLE`) を防止。
*   **GitHub RAW リポジトリからの update.json 直参照**: CDNキャッシュ遅延を解消。
*   **武器テーブルへの入手エリアタグバッジ直接表示**: テーブル一覧内で武器名と一緒に入手エリアを一覧視認可能に改善。
